[tox]
env_list =
    py{310,311,312,313}
minversion = 4.12.1

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg
extras = dev
commands =
    pytest {tty:--color=yes} {posargs}
